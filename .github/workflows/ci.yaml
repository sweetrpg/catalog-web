name: CI

on:
  push:
    branches: [develop]

permissions:
  contents: write

jobs:
  ci:
    uses: sweetrpg/github-actions/.github/workflows/swift-ci.yaml@master
    with:
      swift-version: "6.2"
    secrets: inherit
