---
apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
    name: catalog-web-auth0

spec:
    refreshInterval: "1h"
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: web-auth0
        creationPolicy: Owner
    dataFrom:
<<<<<<< HEAD
        # Same Akeyless path as the dev overlay, i.e. the same Auth0 application/client id. That
        # application's Allowed Callback URLs must additionally include
        # https://sweetrpg-local.curlew-smoot.ts.net/catalog/auth/callback - Auth0 rejects the
        # redirect otherwise, TLS validity notwithstanding.
=======
>>>>>>> 88cfc56 (chore: local deployment (on the correct branch))
        - extract:
              key: /sweetrpg/dev/catalog/auth

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
    name: catalog-web-cache

spec:
    refreshInterval: 1h
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: web-cache
        creationPolicy: Owner
    dataFrom:
<<<<<<< HEAD
=======
        # Reuses the same shared Redis instance's client credentials as catalog-api, since this
        # app's cache is just an optimization over the same catalog-api data, not separate state.
>>>>>>> 88cfc56 (chore: local deployment (on the correct branch))
        - extract:
              key: /sweetrpg/catalog/cache/auth
