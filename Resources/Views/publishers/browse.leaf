#extend("base"):
	#export("body"):
		<section class="page-header">
			<span class="section-label">Browse</span>
			<h2>Publishers</h2>
			<form class="nav-search" action="#(meta.basePath)/publishers" method="get">
				<input class="input" type="search" name="q" placeholder="Search publishers" value="#(query)">
				<select class="input" name="order" onchange="this.form.submit()">
					<option value="asc" #if(orderIsAsc): selected #endif>Name (A-Z)</option>
					<option value="desc" #if(orderIsDesc): selected #endif>Name (Z-A)</option>
				</select>
			</form>
			#if(canEdit):
				<a class="btn btn-primary" href="#(meta.basePath)/publishers/new">Add publisher</a>
			#endif
		</section>

		#if(noResults):
			<p class="empty-state">No publishers match "#(query)".</p>
		#endif

		<div class="card-grid">
			#for(item in items):
				<a class="card" href="#(meta.basePath)/publishers/#(item.id)">
					<div class="card-body">
						<div class="card-title">#(item.name)</div>
						<span class="card-meta">#(item.volumeCountLabel)</span>
					</div>
				</a>
			#endfor
		</div>
	#endexport
#endextend
