#extend("base"):
	#export("body"):
		<section class="page-header">
			<span class="section-label">Browse</span>
			<h2>Publishers</h2>
			<form class="nav-search" action="#(meta.basePath)/publishers" method="get">
				<input class="input" type="search" name="q" placeholder="Search publishers" value="#(query)">
			</form>
		</section>

		#if(noResults):
			<p class="empty-state">No publishers match "#(query)".</p>
		#endif

		<div class="card-grid">
			#for(item in items):
				<a class="card" href="#(meta.basePath)/publishers/#(item.id)">
					<div class="card-body">
						<div class="card-title">#(item.name)</div>
					</div>
				</a>
			#endfor
		</div>
	#endexport
#endextend
